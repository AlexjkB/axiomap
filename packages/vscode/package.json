{
  "name": "@axiomap/vscode",
  "version": "0.0.0",
  "private": true,
  "description": "Axiomap VS Code extension.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/extension.js",
  "types": "./dist/extension.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@axiomap/core": "workspace:*",
    "@axiomap/webview": "workspace:*"
  }
}
